---
title: "MCP Tool Schema"
version: 3.7.0
tool_count: 25
generated_at: "auto"
---

# MCP Tool Schema (v3.7.0)

Mnemosyne exposes **25 MCP tools** for memory management, retrieval, and diagnostics.

---

### 1. `mnemosyne_remember`
Store a durable memory

| Parameter | Type | Required |
|-----------|------|----------|
| `content` | `string` | yes |
| `importance` | `float` | no (default: 0.5) |
| `source` | `string` | no (default: user) |
| `scope` | `string` | no (default: session) |
| `valid_until` | `string` | no (default: ) |
| `extract_entities` | `bool` | no (default: false) |
| `extract` | `bool` | no (default: false) |
| `metadata` | `dict` | no (default: {}) |
| `veracity` | `string` | no (default: unknown) |

### 2. `mnemosyne_recall`
Search memories by vector+FTS hybrid ranking

| Parameter | Type | Required |
|-----------|------|----------|
| `query` | `string` | yes |
| `limit` | `int` | no (default: 5) |
| `temporal_weight` | `float` | no (default: 0.0) |
| `query_time` | `string` | no (default: ) |
| `temporal_halflife` | `float` | no (default: 24) |
| `vec_weight` | `float` | no (default: null) |
| `fts_weight` | `float` | no (default: null) |
| `importance_weight` | `float` | no (default: null) |

### 3. `mnemosyne_forget`
Permanently delete a memory by ID

| Parameter | Type | Required |
|-----------|------|----------|
| `memory_id` | `string` | yes |

### 4. `mnemosyne_get`
Retrieve a single memory by ID (no search)

| Parameter | Type | Required |
|-----------|------|----------|
| `memory_id` | `string` | yes |

### 5. `mnemosyne_update`
Update content or importance of an existing memory

| Parameter | Type | Required |
|-----------|------|----------|
| `memory_id` | `string` | yes |
| `content` | `string` | no (default: ) |
| `importance` | `float` | no (default: ) |

### 6. `mnemosyne_validate`
Attest, update, or invalidate a memory (collaborative ownership)

| Parameter | Type | Required |
|-----------|------|----------|
| `memory_id` | `string` | yes |
| `action` | `enum[attest,update,invalidate,delete]` | yes |
| `validator` | `string` | no (default: ) |
| `new_content` | `string` | no (default: ) |
| `note` | `string` | no (default: ) |
| `bank` | `enum[private,surface]` | no (default: private) |

### 7. `mnemosyne_invalidate`
Mark a memory as expired/superseded

| Parameter | Type | Required |
|-----------|------|----------|
| `memory_id` | `string` | yes |
| `replacement_id` | `string` | no (default: ) |

### 8. `mnemosyne_import`
Import memories from JSON file or provider (Hindsight, Mem0)

| Parameter | Type | Required |
|-----------|------|----------|
| `input_path` | `string` | no (default: ) |
| `provider` | `string` | no (default: ) |
| `api_key` | `string` | no (default: ) |
| `user_id` | `string` | no (default: ) |
| `agent_id` | `string` | no (default: ) |
| `base_url` | `string` | no (default: ) |
| `dry_run` | `bool` | no (default: false) |
| `channel_id` | `string` | no (default: ) |
| `force` | `bool` | no (default: false) |

### 9. `mnemosyne_export`
Export all memories to a JSON file

| Parameter | Type | Required |
|-----------|------|----------|
| `output_path` | `string` | yes |

### 10. `mnemosyne_diagnose`
PII-safe diagnostics: deps, DB state, vector readiness

*No parameters*

### 11. `mnemosyne_stats`
Memory statistics: working count, episodic count, BEAM tiers

*No parameters*

### 12. `mnemosyne_sleep`
Run consolidation cycle (compress old working memories)

| Parameter | Type | Required |
|-----------|------|----------|
| `all_sessions` | `bool` | no (default: false) |
| `dry_run` | `bool` | no (default: false) |

### 13. `mnemosyne_triple_add`
Add a fact triple to the knowledge graph

| Parameter | Type | Required |
|-----------|------|----------|
| `subject` | `string` | yes |
| `predicate` | `string` | yes |
| `object` | `string` | yes |
| `valid_from` | `string` | no (default: ) |

### 14. `mnemosyne_triple_query`
Query the temporal knowledge graph

| Parameter | Type | Required |
|-----------|------|----------|
| `subject` | `string` | no (default: ) |
| `predicate` | `string` | no (default: ) |
| `object` | `string` | no (default: ) |

### 15. `mnemosyne_graph_link`
Declare a semantic edge between two memories

| Parameter | Type | Required |
|-----------|------|----------|
| `source_id` | `string` | yes |
| `target_id` | `string` | yes |
| `relationship` | `string` | yes |
| `weight` | `float` | no (default: 0.5) |

### 16. `mnemosyne_graph_query`
Multi-hop BFS traversal from a seed memory

| Parameter | Type | Required |
|-----------|------|----------|
| `seed_memory_id` | `string` | yes |
| `max_hops` | `int` | no (default: 2) |
| `edge_type` | `string` | no (default: ) |
| `min_weight` | `float` | no (default: 0.0) |

### 17. `mnemosyne_shared_remember`
Store compact cross-agent surface memory

| Parameter | Type | Required |
|-----------|------|----------|
| `content` | `string` | yes |
| `kind` | `string` | no (default: meta) |
| `importance` | `float` | no (default: 0.8) |
| `veracity` | `string` | no (default: unknown) |
| `metadata` | `dict` | yes |

### 18. `mnemosyne_shared_recall`
Search only the shared surface DB

| Parameter | Type | Required |
|-----------|------|----------|
| `query` | `string` | yes |
| `limit` | `int` | no (default: 5) |

### 19. `mnemosyne_shared_forget`
Delete one working shared-surface memory by ID

| Parameter | Type | Required |
|-----------|------|----------|
| `memory_id` | `string` | yes |

### 20. `mnemosyne_shared_stats`
Return shared surface DB path and counts

*No parameters*

### 21. `mnemosyne_scratchpad_write`
Write a temporary note to the scratchpad

| Parameter | Type | Required |
|-----------|------|----------|
| `content` | `string` | yes |

### 22. `mnemosyne_scratchpad_read`
Read the scratchpad entries

*No parameters*

### 23. `mnemosyne_scratchpad_clear`
Clear all scratchpad entries

*No parameters*

### 24. `mnemosyne_remember_canonical`
Store an owner-scoped canonical fact (single source of truth)

| Parameter | Type | Required |
|-----------|------|----------|
| `content` | `string` | yes |
| `importance` | `float` | no (default: 0.5) |
| `veracity` | `string` | no (default: unknown) |
| `source` | `string` | no (default: user) |
| `valid_until` | `string` | no (default: ) |

### 25. `mnemosyne_recall_canonical`
Recall canonical facts by slot, category, or substring

| Parameter | Type | Required |
|-----------|------|----------|
| `slot` | `string` | no (default: ) |
| `category` | `string` | no (default: ) |
| `substring` | `string` | no (default: ) |
| `limit` | `int` | no (default: 5) |
| `include_history` | `bool` | no (default: false) |
